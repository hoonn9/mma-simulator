export type PlayerStyle = {
  baseFight: string;
  etc: string[];
};

export type PlayerGrade = {
  ranking: number;
};

export type PlayerStat = {
  attack: number;
  defense: number;
  hp: number;
};
